# Single Package Test

This is a test plugin for single-package repository mode.

## Features

- Demonstrates single-package mode where `manifest.mf` is in the repository root
- No subdirectories needed for the package
- Simpler structure for single-plugin repositories

## Usage

```bash
test-cmd --output result.txt --verbose
```

## Installation

Use the Command Launcher to install this package.
